# It may be used for any necessary initialization code for the episodes app in the future.

# If any app-specific configuration or initialization is required, it can be added here.
# For example, you might want to import and configure app-specific settings or signals.

# from django.apps import AppConfig

# class EpisodesConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'backend.apps.episodes'

#     def ready(self):
#         import backend.apps.episodes.signals